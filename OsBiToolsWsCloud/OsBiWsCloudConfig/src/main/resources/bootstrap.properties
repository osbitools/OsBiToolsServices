# Configuration Service Id (appId) to report itself in service registry
# Same name should be used by other service to dynamically locate configuration in the cloud
# using parameter spring.cloud.config.discovery.service-id that must be in bootstrap.properties
# For example spring.cloud.config.discovery.service-id=config_service
spring.application.name=osbi_cfg_srv
