# Application Service Id (appId) to report itself in service registry
spring.application.name=osbi_core_app_srv

# Check for configuration service during boot using cloud service registry
spring.cloud.config.discovery.enabled=true
spring.cloud.config.discovery.service-id=osbi_cfg_srv

# Fail fast if cloud configuration service not available
spring.cloud.config.failFast=true
